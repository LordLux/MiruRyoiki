# Tags for selective test execution.
# real-api tests hit the real AniList API and require a valid ACCESS_TOKEN in test/.env.
# Run them with:  powershell -File test/run_real_anilist.ps1
# Skip them with: fvm flutter test --exclude-tags real-api
tags:
  real-api: {}
